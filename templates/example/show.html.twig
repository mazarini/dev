{% extends 'layout.html.twig' %}

{% block title %}
	Example
{% endblock %}

{% block content %}
	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ data.entity.id }}</td>
			</tr>
			<tr>
				<th>Col1</th>
				<td>{{ data.entity.col1 }}</td>
			</tr>
			<tr>
				<th>Col2</th>
				<td>{{ data.entity.col2 }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('example_index') }}">back to list</a>

	<a href="{{ path('example_edit', {'id': data.entity.id}) }}">edit</a>

	{{ include('example/_delete_form.html.twig') }}
{% endblock %}
